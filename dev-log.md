# 세나봇(Sena_Bot) 개발 로그

길드전 공격팀 세팅 가이드 챗봇 — 전체 개발 과정 기록

---

## 1. 프로젝트 개요

**목적**: 게임 길드전 공격팀 세팅 공략을 챗봇 질의응답 + 직접 편집 가능한 웹사이트로 제공. 길드원들이 자연어로 질문하면 관련 덱 정보를 찾아 답변하고, 필요하면 직접 수정도 가능하게 함.

**배포 주소**: `https://senabot-production.up.railway.app`
**GitHub**: `https://github.com/jhpark-92/senabot`

## 2. 기술 스택

| 구성요소 | 사용 기술 |
|---|---|
| 백엔드 | FastAPI (Python) |
| LLM | Google Gemini API (`gemini-3.6-flash`, 유료 전환) |
| 에이전트 도구 연결 | FastMCP (MCP 프로토콜) |
| 프론트엔드 | 단일 HTML/CSS/JS (프레임워크 없음) |
| 배포 | Railway (GitHub 연동 자동 배포) |
| 영구 저장소 | Railway Volume (`/data` 마운트, 500MB) |

## 3. 파일 구조

```
Sena_Bot/
├── main.py               # FastAPI 백엔드 (전체 API)
├── chat_logic.py           # Gemini + MCP 연동 챗봇 로직
├── mcp_server.py            # MCP 도구 서버 (get_guide)
├── static/index.html         # 프론트엔드 전체
├── guide_data.json           # 길드전 덱 공략 데이터
├── raid_data.json            # 강림/파괴신/돌발레이드 공략 데이터
├── history.json             # 통합 수정 이력 (정정 내역 탭)
├── activity_log.json         # 활동 로그 (로그인/챗봇질문/저장, 60일 보관)
├── users.json               # 회원 정보 (아이디, 해시된 비밀번호, 승인여부)
├── memo.json                # 공유 메모장 (admin 전용)
├── abbreviations.json         # 캐릭터 축약어 사전 (챗봇 답변 정확도용)
├── requirements.txt
├── .env                    # GOOGLE_API_KEY, DATA_DIR 등 환경변수
└── update_guide.py           # 대량 텍스트 자료를 구조화해서 guide_data.json에 반영하는 스크립트
```

## 4. 핵심 기능

### 4.1 챗봇 (Gemini + MCP)
- 자연어 질문 → Gemini가 `get_guide` MCP 도구 호출 → 가이드 데이터 조회 → 답변 생성
- 캐릭터 이름 축약어(`abbreviations.json`) 및 "오" 글자 다의성(오공/오르카/오목) 구분 규칙을 시스템 지시에 명시
- 카운터 조합뿐 아니라 장비 세팅/우선순위/메모까지 종합해서 답변하도록 프롬프트 설계
- 대화 기록은 `localStorage`에 저장되어 새로고침해도 유지, "대화 지우기" 버튼 제공

### 4.2 가이드 보기 / 덱 수정
- 마스터-디테일 레이아웃(왼쪽 목록 + 오른쪽 상세)
- 검색은 순서 무관 매칭(글자 정렬 비교) 지원 — "선델오"와 "오선델" 모두 검색됨
- 덱 수정/추가/삭제(CRUD) 전체 지원, 저장 시 로그인 계정으로 권한 확인

### 4.3 레이드 공략 (강림 / 파괴신 / 돌발레이드)
- 강림: 태오/카일/연희/카르마 (목록형)
- 파괴신: 단일 텍스트 페이지 (목록 없음)
- 돌발레이드: 칼리스트라/아스트레아/레오니드 (목록형)

### 4.4 정정 내역 (통합 이력)
- 덱 수정/추가/삭제, 메모 저장, 레이드 저장을 모두 `history.json`에 통합 기록
- 타입별 색상 배지로 구분 표시, 수정자 아이디와 한국시간(KST) 타임스탬프 포함

### 4.5 회원 관리
- 아이디+비밀번호 회원가입 → 관리자 승인 후 로그인 가능
- 비밀번호는 PBKDF2 해시로 저장 (평문 저장 안 함)
- "사용자 관리" 탭(admin 전용): 승인 대기 목록, 전체 회원 목록(삭제 가능)
- admin 계정 로그인 시 별도 관리자 비밀번호 재입력 없이 바로 접근 가능하도록 개선

### 4.6 활동 로그
- 로그인, 챗봇 질문, 저장(덱/메모/레이드) 활동을 전부 기록
- 계정별 필터링 조회 가능
- 60일 지난 로그는 자동 정리

### 4.7 공유 메모장
- admin 전용, 수정 예정 사항을 자유 텍스트로 기록해두는 공간

## 5. 주요 트러블슈팅 & 배운 점

### 5.1 배포/인프라 관련
- **venv 생성 중단**: `python3 -m venv venv` 중간에 Ctrl+C로 끊으면 불완전한 상태가 됨 → 삭제 후 완료까지 대기하며 재생성
- **8000번 포트 충돌**: 여러 프로젝트를 오가며 uvicorn을 여러 번 켜서 발생 → `lsof -i :8000` + `kill -9`로 정리, 또는 `--port` 옵션으로 분리
- **GitHub 인증**: 비밀번호 로그인 차단됨 → Personal Access Token 발급 후 사용. **토큰을 채팅에 그대로 노출한 사고 발생** → 즉시 토큰 무효화 처리
- **.gitignore 실수**: 파일 안에 `echo` 명령어 문장을 그대로 타이핑해 넣어서 규칙이 적용 안 됨 → 파일 내용을 순수 경로만 남기도록 수정
- **Railway GitHub 연동 일시 단절**: "GitHub Repo not found" 발생, 원인 불명확했으나 이후 자연 복구됨

### 5.2 Railway Volume(영구 저장소) 관련
- **핵심 함정**: Volume 연결 후에는 `git push`로 데이터 파일(guide_data.json 등)을 바꿔도 서비스에 자동 반영되지 않음 (`ensure_data_files()`가 최초 1회만 파일을 복사하기 때문)
- **해결**: 데이터 갱신은 항상 API(PUT/POST/DELETE 엔드포인트)를 통해 반영해야 함. 로컬에서 구조화한 데이터를 배포 서버에 반영하려면 `push_to_production.py`처럼 API를 호출하는 스크립트 필요
- **파일명 변경 시 주의**: `deck_history.json` → `history.json`으로 이름을 바꾸면서 예전 데이터가 안 옮겨짐 → `ensure_data_files()`에 1회성 마이그레이션 로직 추가로 해결

### 5.3 MCP / Gemini 연동 관련
- **MCP 세션을 직접 Gemini에 전달하는 방식(`tools=[mcp_client.session]`)은 불안정**: `cannot pickle '_asyncio.Future'`, 스키마 변환 중 `bool` 타입 에러 등 라이브러리 버그 다수 발생 → MCP 도구를 순수 파이썬 함수로 감싸서 전달하는 방식으로 우회
- **MCP 서브프로세스 포트 문제**: Railway 배포 환경에서 `mcp_server.py`가 하드코딩된 포트(8001)로 접속 시도 → 실제 서비스 포트(Railway가 동적 할당)와 달라 연결 실패. `os.environ.get("PORT")`로 해결 시도했으나, fastmcp의 `Client()`가 서브프로세스에 환경변수를 자동 상속 안 함 → `StdioTransport(env=...)`로 명시적 전달해서 해결
- **Gemini API 키 형식 변경**: 2026년 6월부터 `AIzaSy...` → `AQ.Ab...` 형식으로 전환. 최신 SDK와 조합해야 정상 인식됨
- **무료 티어 한도**: 분당 5~15회, 하루 20회 제한에 반복적으로 걸림 → 결제 등록(유료 전환)으로 해결

### 5.4 인증/보안 구조 변경
- **비밀번호 체계가 여러 번 개편됨**:
  1. 1차: 사이트 전체 공유 비밀번호(`SHARED_PASSWORD`) 하나로 시작
  2. 2차: 이름+개인코드 방식 시도
  3. 3차: 게임 스크린샷 인증 시도 (Gemini Vision으로 UID/길드명 추출) — 위조 가능성 문제로 보류
  4. 4차: 자체 회원가입(아이디+비밀번호) + 관리자 승인 방식으로 최종 정착
  5. 5차: "덱 수정 등 저장 기능"의 인증을 `SHARED_PASSWORD`가 아닌 "로그인 여부"로 통합 (`check_login()`)
  6. 6차: "사용자 관리" 탭의 별도 관리자 비밀번호(`ADMIN_PASSWORD`)를 없애고, 로그인 계정이 `admin`인지로 판단하도록 단순화
- **교훈**: 인증 방식은 처음부터 "로그인 = 모든 권한의 기준"으로 설계했으면 더 간단했을 것. 여러 종류의 비밀번호가 혼재하면서 버그(파라미터 누락 등)가 반복 발생했음

### 5.5 프론트엔드 버그
- **한글 입력 중복 전송**: 한글 IME 조합 완료 시 Enter 키다운 이벤트가 중복 발생 → `!e.isComposing` 조건 추가로 해결
- **함수 스코프 실수**: 메모장 저장 로직을 다른 함수(`loadCorrections`) 안에 실수로 중첩시켜서 페이지 로드 시 정상 초기화 안 됨
- **파라미터 누락 반복**: `username`을 변수로는 가져오면서 실제 서버 요청 body에는 빠뜨리는 실수가 여러 곳(덱 수정/메모/레이드)에서 반복 발생 → 이후 `getAuth()` 공통 헬퍼로 통일해서 재발 방지

### 5.6 데이터 구조화
- 원본 비정형 텍스트(엑셀+메모)를 Gemini로 구조화할 때, **한 번에 전체를 넣으면 누락/병합 오류가 발생** → 출처별로 나눠서 개별 처리 후 병합하는 방식으로 개선 (15개 → 100개 항목으로 정확도 향상)
- 표기만 다른 중복 덱(예: "델오(오르카)란" vs "델오란")은 별도 스크립트로 자동 병합
- 대량 텍스트 업데이트는 `guide_updates/` 폴더에 파일을 쌓아두고 스크립트로 일괄 처리하는 방식으로 전환 (재사용 가능한 파이프라인)

## 6. 알려진 이슈 / 다음 할 일

- [ ] **모바일에서 한글 아이디로 회원가입이 안 되는 문제** — 원인 미확인, 추가 조사 필요
- [ ] 가이드/레이드 데이터가 프론트엔드에서 캐싱되어, 다른 사람이 그 사이 수정한 내용이 새로고침 전까지 반영 안 됨 (의도적 설계지만 개선 여지 있음)
- [ ] 사용자 관리 관련 코드(`main.py`, `index.html`)를 admin 이중 인증 제거 방향으로 사용자가 직접 수정 완료 (최종 검증 필요)

## 7. 정리

프로젝트는 "간단한 챗봇 하나"에서 시작해서, 회원 관리·권한 체계·다중 콘텐츠 유형(길드전 덱 + 레이드)·통합 이력·활동 로그까지 갖춘 소규모 커뮤니티 웹 서비스로 성장했습니다. 특히 Railway Volume의 "최초 1회만 복사" 특성과, 여러 인증 방식이 혼재하면서 생긴 버그들이 가장 반복적으로 겪은 트러블슈팅 포인트였습니다.